{
  "$schema": "https://wails.io/schemas/config.v2.json",
  "name": "Anvil Dashboard",
  "outputfilename": "anvil-full",
  "frontend:install": "npm install --ignore-scripts",
  "frontend:build": "npm run build",
  "frontend:dev:watcher": "npm run dev",
  "frontend:dev:serverUrl": "auto",
  "appargs": "dashboard",
  "author": {
    "name": "Ernesto Diaz"
  },
  "info": {
    "deprecated": true,
    "migration": "DASH-MIGR-001: Migrating to standalone Rust + Tauri 2 app (anvil-dashboard). This Wails config will be removed once the Tauri app reaches feature parity. The Go layers (entity, dto, mapper, query, writer) remain in this repo as the shared data contract."
  }
}
